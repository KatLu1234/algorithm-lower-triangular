# 중간 발표 전략 — 시간표 추천 알고리즘

> 발표 흐름: **① 알고리즘 구조 → ② Demonstration → ③ What's next**
> 이 문서는 슬라이드 구성·핵심 메시지·시연 시나리오를 정리한 *발표 설계도*다.
> 모든 기술 내용은 현재 적용된 코드(`app/libs/`, `app/schemas/`) 기준으로 검증됨.

---

## 0. 발표 개요 (먼저 합의할 것)

- **목적**: "이건 단순 CRUD가 아니라 *알고리즘 분해*가 핵심인 프로젝트"임을 증명한다.
- **한 줄 메시지**: 시간표 추천 문제를 **책임 기반 A-B-C 트리**로 분해해, 5개 알고리즘 카테고리(정렬·탐색·탐욕·DP·기타)를 한 파이프라인에 녹였다.
- **시간 배분(15분 기준)**: 구조 6분 · 데모 6분 · 다음 단계 3분.
- **스토리라인**: 문제 → 왜 알고리즘 문제인가 → 트리 분해 → 실제 동작(데모) → 남은 길.

> 발표 톤: 슬라이드는 "그림 + 키워드", 말로 설명을 채운다. 코드는 1~2장만 *핵심 줄*만.

---

## ① 알고리즘 구조 (약 6분 / 5~6장)

### S1. 문제 정의 — "왜 어려운가"
- 후보 강의 N개에서 부분집합을 고르는 **조합 최적화**: 시간 충돌 없이, 학점 한도 안에서, 선호를 최대화.
- 단순 완전탐색은 2^N. 제약(충돌·학점·blackout·필수)과 점수를 동시에 다뤄야 함 → **분해가 필요**.
- 발표 포인트: "스케줄링 + 배낭 + 최단경로가 한 문제에 섞여 있다."

### S2. 큰 그림 — A-B-C 책임 분해 트리
```
[루트] recommend()  — 최적 시간표 추천
 ├── [A] 가능성 분석 (Feasibility)  — "어떤 조합이 hard 제약을 통과하는가"
 ├── [B] 가치 평가 (Valuation)       — "통과한 조합이 얼마나 좋은가"
 └── [C] 선택과 비교 (Selection)     — "무엇을 어떻게 보여줄 것인가"
```
- A·B는 좁→넓(per-course → per-pair → per-set)으로 합성, C는 넓→좁으로 풀어내림 = **divide–conquer–combine** 형상.
- 실제 진입점: `app/libs/timetable.py`의 `recommend()` (A 실패 시 B·C 건너뛰고 InfeasibilityReport 반환).

### S3. A — 가능성 분석 (hard 제약 필터)
- **A-1 강의 풀 정제 (per-course / 해시)**: 제외·그룹 제외 처리, **blackout 슬롯 제거(걸친 슬롯이 하나라도 있으면 강의 통째 제외)**, 필수 잠금.
- **A-2 충돌 관계 (per-pair / 정렬·이진탐색)**: 시간 겹침 + **슬롯별 건물** 기준 이동시간 부족 + 같은 분반 그룹 상호 배타 → 양립 가능성 행렬.
- **A-3 가지치기 (per-subset / 활동 선택)**: 학점 한도 도달 가능성·필수 그룹 충족 검증, B가 소비할 시간순 시퀀스 구축.
- 발표 포인트: "A는 *경우의 수를 줄이는* 단계 — 여기서 못 거르면 B가 폭발한다."

### S4. B — 가치 평가 (점수화 + top-K)
- **B-1 강의 가치 v(c) (per-course / 해시·이진탐색)**:
  ```
  v(c) = 중요도×학점 + 시간대 + 건물(distinct 합) + 카테고리 + 이수요건 + 교수 페널티
  ```
- **B-2 전이 비용 (per-pair / 플로이드-워셜)**: 건물 간 전(全)쌍 최단 이동시간 표 1회 계산 → 이후 O(1) 룩업.
- **B-3 누적 최적화 + top-K (per-schedule / 0-1 배낭 + 백트래킹)**: 그룹당 최대 1분반, 학점 용량 제약, 시간표 단위 후처리 페널티, **배낭 상한으로 가지치기한 백트래킹**으로 상위 K개 복원.
- 발표 포인트: "B-2 최단경로 결과가 B-3 점수의 입력이 된다 — 알고리즘이 서로 먹이사슬."

### S5. C — 선택과 비교 (보여주기)
- **C-1 정렬·다양성 (안정 합병 정렬)**: 점수 내림차순 + 동률 안정 + 다양성 후처리.
- **C-2 쌍 비교 (LCS)**: 후보 시간표 간 공통 백본 + 차이 강의 추출.
- **C-3 포함·배제 사유 색인 (해시)**: 강의별 "왜 들어갔/빠졌나" → 설명 가능성의 핵심 자료.

### S6. 알고리즘 매핑 + 복잡도 (한 장으로 임팩트)
- **현재 실제 연결된 9종**: 해시(A-1·B-1·C-3), 시간정렬(A-2), 이진탐색(A-2·B-1), 활동선택(A-3), 플로이드-워셜(B-2), 0-1 배낭(B-3), 백트래킹(B-3), 안정 합병정렬(C-1), LCS(C-2).
- 5개 카테고리(정렬·탐색·탐욕·DP·기타) **전부 사용**.
- 복잡도: `T = O(V³ + N² + N·C + Ñ²·L²)`, 현실 입력(N=30·V=20)에서 약 11,000 연산 ≈ 1 ms.
- 발표 포인트(솔직하게): "응답 시간 지배항은 알고리즘이 아니라 (예정된) LLM 호출 ~1초."

---

## ② Demonstration (약 6분)

> 목표: "트리가 *말이 되는 출력*을 낸다"를 눈으로 보여준다. 라이브가 불안하면 녹화/스크린샷 백업.

### D1. 데모 환경 띄우기 (30초)
- `docker compose up --build` → 시간표 화면 http://localhost:8080 / API 문서 http://localhost:8000/docs.
- 백업: 위 두 화면 스크린샷 + 미리 받아둔 응답 JSON.

### D2. 정상 시나리오 — top-K 추천 (2분)
- 입력: 강의 후보 5~6개 + 학점 범위 + 강의별 중요도 + 필수 1개.
- 출력에서 짚을 것: ① **상위 K개 시간표**, ② **점수 분해(score_breakdown)** — 중요도·시간대·건물·카테고리·교수 항이 어떻게 합쳐졌는지, ③ **강의별 포함/배제 사유(rationale)**.
- 멘트: "점수가 *블랙박스가 아니라* 항목별로 분해돼 나온다 = C-3의 설명 가능성."

### D3. 제약 시나리오 — blackout 하드 강제 (1.5분) ★최근 반영
- "금요일 종일 공강" blackout을 넣고 재실행.
- 보여줄 것: 금요일에 **한 번이라도** 열리는 강의(월·수·금 강의 포함)가 통째로 빠짐 → 결과 시간표 금요일 완전 공강.
- 멘트: "강의는 슬롯을 쪼개 들을 수 없다 → A-1을 `all`에서 `any`로 바꿔 하드 제약으로 강제(슬롯 단위)."

### D4. Infeasibility 시나리오 — 조기 종료 (1.5분)
- 필수 강의끼리 시간 충돌(또는 필수 강의가 blackout과 충돌)하게 입력.
- 보여줄 것: B·C를 건너뛰고 **InfeasibilityReport** 반환 — `reason`·`stage`·**resolution_hint**("어느 제약을 풀면 가능").
- 멘트: "실패를 *진단*으로 돌려준다 = A-3 조기 종료 경로(§9.8)."

### D5. (선택) 분반·교수 차원 (30초)
- 같은 과목 다른 분반(같은 `course_group_id`)을 넣고, 교수 가중치로 분반이 갈리는 것 한 컷.

---

## ③ What's next (약 3분 / 2장)

### N1. 로드맵 — 짧게 우선순위로
- **LLM 자연어 설명·입력 (Upstage Solar)**: 현재 `explain:true`를 받아도 `explanation:null`. 컨텍스트 빌더(`llm_context.py`)·클라이언트(`llm_client.py`, stdlib `urllib`)는 골격 존재 → 키 연결 + B 결과 → 자연어 설명 채우기. *LLM은 점수를 정하지 않고 입력 해석/출력 설명만 한다(시스템 불변항).*
- **DB 영속화 (Supabase)**: 현재 후보 강의는 매 요청 본문 전달. `claude/server/db/` 설계만 존재(테이블당 1파일 DDL) → 연결 시 `items` 라우터·`deps` 복구.
- **실제 건물 거리표**: 지금은 후보 강의에서 자동 추출 + 다른 건물 5분 기본값 → 실측 도보 시간 행렬로 교체(플로이드-워셜 입력 품질 ↑).
- **고급 가중치 UI 노출**: 카테고리·이수요건·건물·교수 가중치, 다양성 λ₃, 연강/공강 선호는 엔진엔 있으나 폼 미노출 → 입력 UI 추가.

### N2. 기술 부채 / 정직한 한계 (신뢰를 주는 슬라이드)
- **spec ↔ 구현 정합**: B-3 spec은 "행렬 경로 DP"지만 실제는 **백트래킹+배낭 상한**. `matrix_path_dp.py`는 현재 dead code → 명세 일치 또는 제거 필요.
- **옵션 알고리즘 미연결**: 다익스트라·위상 정렬·편집 거리는 자리만 있고 미구현(필요성 재검토).
- **top-K 완전성**: K ≤ 15까지 naive 백트래킹, 그 이상은 우선순위 큐 도입 필요.
- 멘트: "무엇이 *검증됐고* 무엇이 *설계뿐*인지 구분해서 보여주는 것 자체가 점수 포인트."

---

## 부록 — 발표 팁

- **그림 1장으로 끝내기**: S2의 A-B-C 트리는 발표 내내 재등장하는 *지도*로 쓴다(현재 강조 노드 하이라이트).
- **데모는 결과 화면 중심**: 코드 스크롤 금지. 점수 분해·사유·hint 세 가지만 손가락으로 짚기.
- **"현재 적용/예정" 색 구분**: 적용된 9 알고리즘은 진하게, 예정(LLM·DB·옵션 알고리즘)은 흐리게.
- **예상 질문 대비**: ① "왜 배낭인가?"(학점=용량) ② "최단경로를 왜?"(건물 간 이동시간 페널티) ③ "LLM이 결과를 바꾸나?"(아니오, 해석·설명만).

---

## 슬라이드 체크리스트 (빌드용)

| # | 제목 | 핵심 1줄 | 시각 자료 |
|---|------|----------|-----------|
| 1 | 문제 정의 | 조합 최적화 = 스케줄링+배낭+최단경로 | 충돌나는 시간표 그림 |
| 2 | A-B-C 트리 | 책임 기반 3분할 | 트리 다이어그램 |
| 3 | A 가능성 | hard 제약 필터(슬롯 blackout 포함) | A-1·2·3 흐름 |
| 4 | B 가치 | v(c) 점수식 + 플로이드-워셜 | 점수식 박스 |
| 5 | C 선택 | 정렬·LCS·사유 색인 | top-K 카드 |
| 6 | 매핑·복잡도 | 9 알고리즘 / O(V³+N²...) | 매핑 표 |
| 7 | 데모 정상 | top-K + 점수분해 + 사유 | 결과 화면 |
| 8 | 데모 blackout | 금요일 공강 하드 강제 | before/after |
| 9 | 데모 infeasible | 진단 hint 반환 | 에러 응답 |
| 10 | What's next | LLM·DB·거리표·UI | 로드맵 |
| 11 | 한계·부채 | spec↔구현, 옵션 미연결 | 체크리스트 |
