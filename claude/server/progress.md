# server/progress.md — 서버 영역 진행 이력

> **갱신 규칙 (반드시 지킬 것)**
> 이 파일은 **다음 두 경우에만** 갱신한다.
> 1. **사용 기술이 바뀔 때** — 프레임워크·DB·LLM SDK·검증 라이브러리 교체/추가/제거.
> 2. **별도의 지침이 추가될 때** — `claude/base/`의 일반 규칙과 별개로, 서버에만 해당하는 결정이 굳어질 때.
>
> 일반 코드 변경(라우트 추가, 알고리즘 구현, 버그 수정, 리팩터링)은 **여기 기록하지 않는다**.
> 그런 변경은 git 커밋 메시지로 충분하다. 본 파일은 "이 영역의 토대가 어떻게 형성됐는가"의 기록이다.

## 1. 현재 사용 기술 스택 (스냅샷)

| 분류 | 선택 | 비고 |
| ---- | ---- | ---- |
| 언어 | Python 3 | `.venv/` 가상환경 사용 |
| 웹 프레임워크 | FastAPI | `app/main.py` 진입점 |
| ASGI 서버 | (TBD, 보통 `uvicorn`) | 실행 명령 확정 시 기록 |
| 검증 / 모델 | Pydantic v2 | `app/schemas/` |
| 미들웨어 | Starlette CORS | `allow_origins=["*"]` (개발용) |
| DB | Supabase | `app/db/supabase.py` |
| LLM 호출 단일 진입점 | (계획) `app/libs/llm_client.py` | 공급자 확정 시 기록 |
| LLM 프롬프트 조립 | (계획) `app/libs/llm_context.py` | `claude/llm-include/` 자료를 읽음 |
| 설정 / 시크릿 | `app/core/config.py` + `.env` | `.env`는 Git 제외 |
| 테스트 프레임워크 | (계획) `pytest` | `tests/` 폴더는 아직 없음 |

## 2. 별도 지침 (server에만 해당)

`claude/base/` 일반 규칙으로 안 풀리는, 서버 영역에 한정된 결정을 한 줄씩 적는다. 비어있어도 무방.

| 항목 | 결정 | 결정일 | 사유 |
| ---- | ---- | ------ | ---- |
| (예) 라우트 함수 시그니처 | 모든 라우트는 비동기(`async def`) | TBD | LLM 호출이 IO-bound이므로 |
| | | | |

## 3. 변경 이력

가장 최근이 맨 위. **기술 스택 또는 별도 지침** 변경만 기재.

| 날짜 | 항목 | 변경 | 사유 / 트리거 |
| ---- | ---- | ---- | ------------- |
| 2026-05-17 | 지침 추가 | `server/tasks.md` 신설 — 서버 영역 작업 칸반(TODO/DOING/DONE) + 카드 형식 + 영역 간 승격 기록. `team-guide.md` §2 시작 전 읽기/§7 빠른 참조/§8 다음 문서에 진입점 연결 | 팀원 작업 확인 보드 도입 |
| 2026-05-17 | 지침 추가 | `server/team-guide.md` 신설 — 서버 팀원이 처음 영역에 진입할 때 읽는 큰 틀 문서(책임·기술 스택 인용·할 일 매핑·Claude 사용 패턴·지침 요약·빠른 참조) | 영역 팀원 온보딩 통일 |
| 2026-05-17 | 진행 파일 | `server/progress.md` 신설, 현재 스택 스냅샷 기록 | "각 scheme에 진행상황 파일" 지시 |
| (2026-05-07 추정) | DB | Supabase 클라이언트(`app/db/supabase.py`) 도입 | 코드 히스토리상 추정 — 정확 일자/사유는 채워 넣을 것 |
| (2026-05-05 추정) | 골격 | FastAPI 골격(`app/main.py`, `api/`, `crud/`, `schemas/`) 생성 | 프로젝트 초기 셋업 |

## 4. 변경 유형 분류

| 유형 | 의미 |
| ---- | ---- |
| 스택 추가 | 새 라이브러리·서비스 도입 |
| 스택 교체 | 기존 선택을 다른 것으로 변경 |
| 스택 제거 | 의존성 제거 |
| 지침 추가 | server 한정 규칙이 새로 굳어짐 |
| 지침 폐기 | 더 이상 적용되지 않는 규칙 제거 |
